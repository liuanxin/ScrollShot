# 验证记录

设备: Pixel 9a, Android API 37, 1080 × 2424. 日期: 2026-09-23.

- 本地 Gradle ZIP SHA-256 校验通过, 使用 SDK AAPT2 离线构建 Debug 与测试 APK 成功, 最新 Debug 已安装.
- 10 项核心测试通过, 使用本机 Kotlin 编译器与缓存 JUnit 执行. 未执行完整 Gradle 测试任务或 lint.
- 无 Launcher 入口, 快捷设置磁贴启动及长按设置入口已验证.
- 100 段测试页自动滚动到底, 保存 1080 × 30848 PNG. 首屏与全部 100 段正文采样区域对比参考内容, 差异像素为 0.
- X 长文从顶部捕获 1080 × 4837, 保留标题、作者与正文开头. 进入持续播放视频区域后安全暂停并保留完成部分, 未宣称已截完包含视频的页面.
- X 草稿裁剪后为 1065 × 4669, 显示 2.86 MB, 保存 2995256 字节. 文件名 20260923-172742-535083.png, 位于 Pictures/ScrollShot.
- 最新版本捕获中显示淡蒙层与停止条. 点击顶部停止后生成 1080 × 5844 草稿, 原因是已手动停止, 预览不含蒙层与停止条.
- 真机导出测试通过: 120000 像素输入缩小到 60000 像素高, 跨分段裁剪逐像素一致, 取消时删除半成品, KB/MB 格式化正确.

## 验证边界

- 蒙层仅提示捕获状态, 不会暂停其他应用的视频或动画. 持续变化会暂停捕获, 不能保证所有动态页面可拼接.
- 已测试内置长文与当前 X 长文, 其他应用、多滚动区域及完全自绘页面仍需实际验证.
- 尚未实际自动滚动至 120000 像素或运行至 10 分钟上限; 超长导出由真机合成分段数据验证.
- 超长 PNG 在不同相册中的打开和缩放能力未全面验证.
- 捕获期间不要运行 uiautomator dump, 它会干扰无障碍服务.

## 滚动节奏和图标优化

- 去除拼接后固定 350ms 等待, 滑动后首次检测由 400ms 提前至 100ms; 截图请求至少间隔 260ms, 持续变化或限频重试受约 3 秒预算约束.
- 匹配预计算纹理行, 提前排除必然不满足阈值的候选, 原匹配分数和歧义阈值保持不变. 10 项核心测试通过, 160 组随机、空白、噪声和错行数据与旧算法的结果完全一致.
- 同一设备测试页, 仅等待优化版连续段周期中位数约 2.94 秒, 加入匹配优化后约 2.65 秒. 此数据不是跨应用性能保证.
- 新版重新截取完整 100 段, 输出 1080 × 30848, 首屏和 100 段正文采样区域差异像素均为 0.
- 圆角纸张向下箭头图标已在快捷面板显示; 顶部停止条采用圆角描边, 点击区域由 36dp 增高至 44dp.
- 真机长按磁贴成功进入设置页, 点击新停止条生成 1080 × 10119 草稿并记录已手动停止; 测试结束确认无障碍服务保持连接.

## 第二轮提速与图片质量选项

- 分段计时定位上一版 260ms 请求间隔触发系统限频, 修正为 350ms. 半屏滚动仍保留重叠, 100 段真机运行未出现截图限频错误.
- Pixel 9a 同测试页, 连续匹配周期中位数约 1.99 秒, 上一版约 2.65 秒; 单步实际位移从 855 增至 1091 像素.
- 新版完整 100 段高清输出 1080 × 30848, 首屏与正文采样差异像素均为 0.
- 默认 JPG(质量 85), 高清开关使用 PNG, 编辑页切换后重新计算实际文件大小. 普通模式上限 1600 万像素, 随设备堆上限进一步收紧; 内存不足时恢复重试按钮, 可缩小裁剪或切到流式高清.
- 同一 X 长图, 两种输出尺寸均为 1080 × 4837, PNG 3014392 字节, JPG 706722 字节, 本例减少 76.6%. 不保证所有内容有相同比例.
- 真机导出测试通过 PNG/JPEG 超长尺寸、JPEG 跨分段裁剪平均通道误差、取消清理和大小格式化. 原 PNG 跨分段裁剪逐像素一致.
- 最终 APK 安装后手动停止与高清保存通过: PNG 1080 × 8970; 普通 JPG 已保存相册, 扩展名与模式一致. 测试结束确认长截图无障碍服务保持连接.

## 裁剪操作栏简化

- 底部统一为取消、重置、高清、保存四个圆角按钮, 高清开启时高亮, 保存使用同一套高亮样式.
- 删除停止状态与操作说明文字, 增加图片预览空间. 深色编辑页状态栏使用浅色图标.
- 离线构建安装成功, 真机检查按钮间距、尺寸、高清切换与保存状态.

## 顶部格式切换与简洁操作栏

- 最终交互默认 JPG, 点击顶部格式名称在 JPG/PNG 之间切换, 不再使用高清按钮.
- 顶部只显示格式、尺寸、实际大小; 底部只有取消、重置、保存, 保留统一圆角样式.
- 每次切换重新编码并更新尺寸/大小, 保存时检查缓存的格式与裁剪范围, 文件后缀和 MIME 随选择同步.
- 离线 Debug 与测试 APK 构建通过. 编码沿用已经真机验证的 PNG/JPEG 路径.
- 真机已确认顶部 JPG/PNG 双向切换并更新实际大小, 无障碍服务保持连接.

## 点击入口无反应修复

- 真机复现: X 列表底部的滚动节点只暴露 ACTION_SCROLL_BACKWARD(8192), 旧逻辑仅接受向下/向前动作, 因而拒绝启动. 同时系统屏蔽 Toast, 失败原因不可见.
- 识别滚动区域时接受向上/向后动作, 捕获方向仍保持向下. 快捷面板收起后, 节点未就绪可单独短暂重试.
- 原 X 底部页面连续两次启动成功, 一次实际点击快捷面板图标, 自动结束后得到当前屏 1080 × 2424 的可裁剪图.
- 不可启动时使用 3.5 秒非触摸提示条, 真机在设置页启动已看到原因提示. 新请求与服务销毁会清理提示条.
- 最新 Debug 离线构建、安装成功, 服务保持连接.
